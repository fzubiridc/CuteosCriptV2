# AGENTS.md — punto de entrada para agentes/devs

Juego **Godot 4.6** isométrico 2D tipo Diablo-like / dungeon-crawler. Rama de trabajo: **`master`** (los commits recientes viven acá; `iso-merge` quedó atrás — histórico).
Port a Godot del original en Pixi (el pixi es legacy; este proyecto ya va por delante).

## Leé esto antes de tocar (en orden)
1. **`docs/project_memory.md`** — estado confirmado, decisiones tomadas, fixes que NO revertir, lista "no tocar". (Actualizado al 2026-06-27.)
2. **`docs/architecture_notes.md`** — cómo funciona cada sistema (escena, dungeon y sus módulos, muros, niebla, luz, IA, audio, FX). (Actualizado al 2026-06-27.)
3. Planes puntuales (algunos parcialmente implementados): `docs/spell_fx_plan.md`, `docs/visibility_darkness_plan.md`, `docs/PLAN_NARRATIVO.md`, `docs/lore/`.

> `docs/code_audit_2026-06-26.md` y `docs/cleanup_candidates_2026-06-26.md` son **snapshots de la revisión del 26-jun**, históricos: su deuda principal (god-objects `dungeon.gd`/`hud.gd`, path 2.5D muerto, director static) **ya se resolvió**. No los leas como estado actual.

## Mapa rápido del código
- **`scripts/`** — lógica. Núcleo: `main.gd` (flujo de pisos), `player.gd`, `enemy.gd`, `boss.gd`, `projectile.gd`, `hud.gd`.
- **`scripts/dungeon.gd`** — el nodo del piso: RENDER iso (piso/muros/occluders/variantes/puertas) + nav (AStarGrid) + API pública + orquesta `generate()`. **Partido en 3 módulos** `RefCounted` que operan sobre él (`d.*`, creados lazy):
  - `scripts/dungeon_gen.gd` (**DungeonGen**) — procgen: grid, salas iso (paralelogramos), grafo MST+loops, roles, spawn/exit, puertas.
  - `scripts/dungeon_decor.gd` (**DungeonDecor**) — antorchas + fogatas.
  - `scripts/dungeon_fog.gd` (**DungeonFog**) — niebla de guerra + reveal de fachadas.
  - Estado compartido que vive en `dungeon.gd` (lo leen render/minimap/enemy/manager): `grid`, `rooms`, `cell_seen`, `_room_of`, `_room_front`, `_front_src`.
- **`scripts/ui/`** — `shop_panel.gd` (ShopPanel) + `inventory_panel.gd` (InventoryPanel); `hud.gd` los coordina.
- **`scripts/autoload/`** — `rng`, `game_state`, `data`, `items`, `audio`, `light_cfg`, `light_field`, `fx_materials`, `save_system`, `fps_counter`, **`combat_director`** (tope de atacantes simultáneos). Registrados en `project.godot [autoload]`.
- **`scenes/`** — `main.tscn` (la de producción), `player`, `enemy`, `boss`, `projectile`, `pickup`, `floater`, `campfire`, `menu`, + sandboxes (`closed_room_test`, `iso_test`, `light_test`).
- **`shaders/`** — `wall_face.gdshader` (caras de muro + entidades, foot-lit unshaded).
- **`tools/`** — tooling web propio. Server único `py tools/serve.py` (puerto 8765, auto-guarda) + `rigtool.html` (rig de varas/anims/bolts + escala) + `rig_sync.py` (converter → `data.gd`) + `aoe_glyph_tool.html` + `index.html` (tabs). El pixi se sirve en `/pixi/`.
- **`assets/`** — tiles iso, fx, sfx, hero/staffs, mobs, ui (incluye `ui/loading.png` = boot splash), `story/` (intro narrativa, pendiente).

## Reglas duras
- Arranque = **`scenes/menu.tscn`** (menú de inicio: título + tarjeta animada del Archimago + récords/continuar; al jugar hace `change_scene_to_file` a `main.tscn`). Escena de juego = **`scenes/main.tscn`**; `ISO=true` es el único path vivo; `Dungeon.use_test_map` debe quedar en **false**.
- **No tocar `addons/godot_ai/`** (vendor/plugin).
- **Commits/push: solo cuando Felipe lo pide** (no por iniciativa propia).
- **Validación:** Felipe prueba jugando (más rápido). Los agentes validan con **compile-check headless** (`godot --headless --path . --import`, exit 0 + sin `SCRIPT ERROR`/`Parse Error`); **no abrir el juego ni screenshotear** salvo que Felipe lo pida.
- Iluminación / antorchas / visibilidad / procgen / player / enemigos / HUD son **sensibles**: cambios incrementales + test visual.
- **Oscuridad = ausencia de luz** (no overlay). El fog NO apaga caras de muro visibles desde el cuarto activo.
- **Muros = `WallSegment`** (fuente lógica): NW/NE traseros, SE/SW fachada; `neighbor()` depende de la **paridad de fila** (TileSet STACKED). Salas = **paralelogramos** (`carve_iso_room`), no rects cartesianos.
- Refactor de un módulo grande → mové la lógica preservando la API pública (otros archivos dependen de `dungeon.*`, etc.) y dejá un wrapper.

## Protocolo de cierre (Definition of Done)
Al cerrar cualquier tarea que cambie código del juego (`scripts/`, `scenes/`, `shaders/`, `project.godot`):
1. Mirá el `git status` real de lo que tocaste.
2. Cambió el **estado** del juego (feature, sistema, bug vivo, deuda saldada) → actualizá `docs/project_memory.md` y el bloque **"Estado en una línea"** de abajo.
3. Cambió **cómo funciona** un sistema (no solo qué hace) → actualizá `docs/architecture_notes.md`.
4. Decisión tomada o revertida → anotala en `docs/project_memory.md` (decisiones / "no tocar").
5. **Convención de nombres:** los docs vivos van **sin fecha** (`project_memory.md`, `architecture_notes.md`) — editá el archivo vivo, nunca crees una copia fechada. Solo los snapshots históricos llevan fecha (`code_audit_2026-06-26.md`, `cleanup_candidates_2026-06-26.md`) y no se actualizan.

Si el cambio es trivial o no altera nada documentado, **decilo en una línea y cerrá** — no inventes updates ni marques como hecho lo que no está en el repo. Un Stop hook (`.claude/hooks/check-memory.mjs`) te recuerda esto si tocás código sin tocar `docs/`.

## Estado en una línea
**Sesión 2026-07-01 (commiteada, master 8+ commits sin pushear→ ahora pusheado) — Z-ORDER DINÁMICO de muros + CUTAWAY suave + fix del FREEZE histórico.** Tanda larga, todo en `scripts/iso_wall_painter.gd` (`IsoWallPainter`, módulo lazy extraído de dungeon.gd, opera por `d.*`).
- **FREEZE "después de matar un mob" RESUELTO** (era el bug intermitente histórico). Causa (hallada con logging pesado + la pista de Felipe "un drop trae problemas"): al recoger XP subís de nivel → `player._open_upgrade` PAUSA el árbol y abre `up_panel`, PERO el **HUD estaba `visible=false`** → el panel (hijo del HUD) no se dibuja → pausado + sin panel + ESC deshabilitado = trancado. El HUD quedaba oculto porque perdía `mode_changed(PLAY)` (si el mode ya era PLAY al conectar, `set_mode` no re-emite). Fix 3 capas: (1) el HUD sincroniza su visibilidad al mode ACTUAL en `_ready`; (2) `_on_level_up` fuerza `visible=true`; (3) watchdog `_any_modal_visible` devuelve false si el HUD está oculto → auto-recupera. Queda logging `[levelup]`/`[loot]` + `GameState.last_pause_src`. (commit `1f67ea2`)
- **Z-ORDER DINÁMICO de muros** (patrón de `DungeonProps.update_occlusion`): las capas fijas front(z+1)/back(z-1) fallaban cuando estabas del "lado equivocado" (aparecías delante de un muro trasero, etc.). Ahora `IsoWallPainter.update_cutaway` decide POR-FRAME el z de cada muro vs el player (detrás→+1, delante→-1), POR-RUN (todos los tiles de un muro flipean juntos, sin escalera — usa los spans de `_merge_wall_spans` vía `_index_wall_depth`/`_wall_depth_index` con `key`/`ra`/`rb`). Divisores + puertas de región + sub-cuartos: todos por el mismo sistema. **Cutaway por-píxel** (hueco elíptico en `wall_face.gdshader`, uniforms `cutaway_*`) con **fade temporal** (`cutaway_strength` lerpeado) + rango contiguo. **Knobs en vivo** (panel L, grupo "Cutaway (muros)": min/rx/ry/up/fade/range, en `LightCfg.DEFS`).
- **NAV:** los mobs respetan los muros de sub-cuarto (`_mark_subroom_nav_solid`); `main._spawn_enemies` re-tira el spawn si `is_nav_solid` (no atrapa mobs). **Divisor:** las puntas vuelven a topar muro (revertido el freno por región que las dejaba flotando). **Minimapa:** divisores como líneas + puertas como línea roja + forma de rombo iso. **place_torches** arreglado (usa muros iso reales). **Contador de perf** ampliado (Fps/F3: CPU/física/GPU-vsync/draws/VRAM). **Mapa de prueba** `scenes/cutaway_test.tscn` (sub-cuartos + divisor + panel L, sin RNG).
- **⚠ PENDIENTE / RETOMAR OTRO DÍA — artefacto de DOBLE-ALPHA en solapes del cutaway:** los tiles de muro se solapan; cuando ambos se transparentan, el solape muestra la cara oscura tapada (doble-alpha). Se intentó el fix "correcto" = **CanvasGroup** (componer opaco → hueco al conjunto). El reparent + la detección ANDABAN (confirmado 11 muros al grupo), pero el **render del CanvasGroup con shader custom NO aplicaba la transparencia** y sin poder ver la pantalla (agente headless) no fue debuggeable → **REVERTIDO** a la versión per-sprite (commit `306dd33`). Mitigación actual: subir `cutaway_min` (menos transparente). Para reintentar: el problema está SOLO en el render del CanvasGroup (¿premultiplicado? ¿world_pos del quad vs global? ¿custom material en CanvasGroup?), no en la lógica.
- **⚠ GOTCHA:** `cutaway_min` se guarda en `user://light_knobs.json`. Felipe lo dejó en **1.0** (= opaco) tuneando → "no veo transparencias" aunque el código ande. Fix: panel L → bajar "Cutaway: opacidad centro" a ~0.3, o borrar el json para resetear.

**Sesión 2026-06-30.ter (sin commitear) — REGIONES Etapa 3 ✅: PUERTAS REALES en las aristas "door" (4 caras).** Antes una arista `edge_features=="door"` era un HUECO caminable; ahora materializa una **puerta real** (sprite cerrada/abierta + colisión + nav-solid + abrir con **clic derecho** + proximidad), reusando la maquinaria de puerta de **DungeonDividers** (una sola implementación). Flujo: `_build_wall_segments` junta las aristas "door" en **`_region_door_edges`** (dedupe por par canónico de celdas, `_pair_key`); `_place_region_doors()` (en `generate()`, tras `_place_dividers`) llama a **`DungeonDividers.spawn_region_door(cell, side)`** que deriva muro base + geometría de la arista y reusa `_add_door`. **Puertas por las 4 CARAS:** Felipe creó el arte faltante (`DoorSE/SW` + `OpenDoorSE/SW`); `_ensure_door_sources` ahora mapea cada cara a su PNG propio (`DOOR_ART_BY_SRC`, antes SE reusaba DoorNW y SW DoorNE — mirror hack) y `_add_door` deriva textura/colisión por sufijo de cara (`_src_suffix`). Cleanup de regen: los holders de puerta de región viven en `_region_door_holders` (liberados en `DungeonDividers.clear()`). **Compila headless OK** (exit 0, 0 errores; los 4 PNGs nuevos importados). Probado end-to-end con el demo `DEBUG_SUBROOM` (ahora emite una puerta REAL, no un hueco). **Limitaciones/follow-ups:** (a) puertas de fachada (SE/SW) cutaway → **✅ ARREGLADO** (`_place_region_doors` suma el holder de las puertas SE/SW a `_front_walls` → `_update_wall_cutaway` las transparenta como las fachadas de muro); (b) colisión de puerta ABIERTA en SE/SW falta en `wall_collision.json` (`open_door_se/sw`) → al abrir cae al fallback "toda la celda pasable" (Felipe puede dibujar esos polígonos en la tool); (c) Etapa 4 (generar sub-cuartos en PROCGEN) → ✅ ver abajo. **Audio:** `toggle_door` ahora reproduce `Audio.play_at("door", holder)` (SFX posicional al abrir/cerrar, divisor y región). **Falta que Felipe lo pruebe jugando.**

**Sesión 2026-06-30.ter (cont.) — REGIONES Etapa 4 ✅: SUB-CUARTOS en procgen real (retira el demo).** `_mark_subroom_demo`/`DEBUG_SUBROOM` REEMPLAZADOS por **`_place_subrooms()`** (corre en `generate()` tras `assign_roles`, antes de `_build_wall_segments`). Por cada sala grande (w,d ≥ 6, no la de spawn) con prob. `SUBROOM_CHANCE=0.35` (seedeado por piso): carva un sub-cuarto RECTANGULAR de 2-3 celdas en una esquina al azar, le da región propia (`SUBROOM_REGION_BASE=90000 + ri`) y marca su frontera INTERIOR con la sala como aristas "wall" en `edge_features` + UNA "door" → muros y puerta EMERGEN solos del pipeline (`_build_wall_segments` → sprites+colisión vía `_wall_segments`/`_install_wall_collisions`; puerta real vía `_place_region_doors`→`spawn_region_door`). Guards: descarta si alguna celda no es piso o es spawn/exit, o si no hay frontera interior (todo perímetro). **`_place_dividers` saltea las salas con sub-cuarto** (`_subroom_rooms`) → un divisor no cruza un sub-cuarto. **Compila headless OK** (exit 0). **Nav ✅ (2026-06-30.ter):** las celdas de sub-cuarto se marcan SÓLIDAS en el AStar de mobs (`_mark_subroom_nav_solid` tras `_build_iso_nav`, celdas en `_subroom_cells`) → los mobs no cruzan los muros de sub-cuarto (ya respetaban divisores/puertas). Guard anti-atrapado: `main._spawn_enemies` re-tira la celda de spawn si `dungeon.is_nav_solid(cell)` (evita spawnear un mob adentro de un sub-cuarto o sobre un muro). **Ojo:** un sub-cuarto puede encerrar un cofre/mercader → alcanzable abriendo la puerta (clic derecho). **Falta que Felipe lo pruebe jugando** (variedad/densidad; ajustar `SUBROOM_CHANCE`/tamaño si hace falta).

**Sesión 2026-06-30.ter (cont.) — WATCHDOG anti-soft-lock + diagnóstico del FREEZE histórico.** Felipe reportó un freeze intermitente "después de matar un mob" (ya le pasó antes; NO lo causan las puertas). Diagnóstico (proceso vivo + logs de Godot): **NO es crash ni loop infinito** (CPU no clavada, ventana "responding", cero errores de script al momento del freeze) → es el **árbol PAUSADO (`get_tree().paused=true`) sin panel visible** y sin salida (ESC se ignora mientras `up_panel`/`death_panel` están "visibles" — `hud._process` early-returns, línea ~209). Descartado por análisis estático: level-up (`_open_upgrade` está limpio: panel centrado por anclas, 6 upgrades válidos), loop de `gain_xp` (fórmula `8+(lvl-1)*6` siempre ≥2). Causa exacta NO identificada (el freeze no deja rastro en el log = pausa silenciosa). **Fix = watchdog** (`hud._process`, corre pausado por `process_mode=ALWAYS`): si el árbol está pausado y `_any_modal_visible()` es false por >1.5s → loguea `_pause_debug_state()` y **auto-despausa** → convierte el hard-lock en un hipo de 1.5s Y captura el contexto para la próxima. **Rastreo de fuente:** cada sitio que hace `get_tree().paused = true` setea antes `GameState.last_pause_src` (`upgrade`/`pause_menu`/`death_win`/`shop`/`inventory`/`skills`) → el dump del watchdog lo incluye → **nombra al culpable** (no adivinamos). `_pause_debug_state()` = last_pause_src + todos los paneles (visible) + mode + lvl + kills. `_any_modal_visible` cubre TODAS las fuentes de pausa (mejora/pausa/muerte/inventario/tienda/habilidades) → si agregás una pausa nueva, sumala ahí. Se agregó `is_open()` a `ShopPanel` y `SkillBar`. **Compila OK.** **Bug aparte (NO tocado, sensible):** `player._apply_light_tex` tira "Parameter t is null" (51× en el log) — solo desde el panel debug de Luz (tecla L) vía `lighting_debug`→`_on_light_cfg_changed`, NO en juego normal; no-fatal. Dejado como issue separado (no toco lighting sin test visual de Felipe).

**Sesión 2026-06-30.bis (sin commitear) — ARQUITECTURA DE REGIONES (migración por etapas, en curso):** Felipe quiere layouts más ricos que salas+corredores (cuartos-en-cuartos, depósitos). Modelo: muros/puertas por **FRONTERA DE REGIÓN** (no solo piso↔EMPTY). **Etapa 1 ✅:** `region_id[y][x]` (paralelo a `grid`: -1 EMPTY, 0..R-1 salas de `_gen_room_cells`, R.. componentes de corredor por flood-fill), se construye al final de `gen_grid` (`_build_regions`). Validado: cobertura 100% (20 salas + 17 corredores). **Etapa 2 ✅ core:** `dungeon.edge_features` (arista "x_y_side" → "wall"/"door") + `_build_wall_segments` ahora emite muro en piso↔EMPTY **O** frontera de región marcada "wall" en `edge_features` (default vacío → idéntico, NO-breaking; verificado 632 segs). Probado end-to-end: **sub-cuarto demo** (`_mark_subroom_demo`, gateado por `DEBUG_SUBROOM`) marca una sub-región en una esquina → muros + 1 puerta EMERGEN solos del pipeline normal. **Bug del divisor ARREGLADO** vía regiones: `plan_divider` ahora frena el crecimiento al cambiar de región (`_region_at`/`region_id`) → no se escapa al corredor (medido: 12 divisores, 0 escapados). **Falta (Etapas 3-4):** puerta real (sprite+colisión) en los huecos "door" + puertas por intención; generar sub-cuartos de verdad en procgen (hoy demo). `_mark_subroom_demo`/`DEBUG_SUBROOM` son TEMPORALES. Detalle en `architecture_notes.md` (§ regiones). **BUG `canvas_item_add_polygon: triangulation failed` RESUELTO** (preexistente, plagó toda la sesión): `_install_wall_collisions` creaba ~28 `CollisionPolygon2D`/piso con puntos consecutivos casi-coincidentes (~0.0002, aristas de largo cero) de los `Geometry2D.merge_polygons`/`offset_polygon` → el renderer falla la triangulación al dibujarlos con "Visible Collision Shapes". Fix: **`_clean_poly`** (saca vértices coincidentes antes de asignar, no cambia la física) + **`_poly_valid`** guard (descarta colineales por área + self-intersecting) en los 3 `cp.polygon=`. Boot: 28→0 errores. Diagnosticado con el **delta de `dropped_count`** del log MCP (el `total_count` se clava y engaña; el método de re-llamar pasos de `generate()` y medir el delta es el confiable). Era solo-debug (en build normal no dibuja colisión).

**Sesión 2026-06-30 (sin commitear):** **suavizado de muros "≥1 tile recto entre giros"** en procgen (`dungeon_gen._smooth_double_corners`, corre en `gen_grid` tras `_remove_thin_walls`; toggle `DEBUG_NO_SMOOTH`). La regla NO es "vértices de esquina cerca" (eso lo entendí mal 2 veces, Felipe corrigió) sino el **camino del muro**: tras un giro 90°, el tramo recto debe ser ≥2 tiles. Métrica = largo de **WALL SPANS** (aristas colineales); **span de largo 1** = la W/S/C/escalera. Diagnóstico: ~90% en CORREDORES (salas iso ya rectas). Algoritmo = **hill-climbing dirigido**: rellena/talla las celdas de cada span-1 y acepta SOLO si baja el total (un toggle ciego EMPEORA; el greedy nunca). Guard: no toca salas ni desconecta (flood desde región mayor). **spans-1 ~37 → ~6 (–85%)**, conexo, verificado en vivo en mapas frescos. **Perf:** conteo INCREMENTAL (`gcount`+`L[0]`, ~8 aristas por toggle O(1) vía tablas `backside`/`ndelta`/`lb` precomputadas, hot loops inline sin method-calls) → ~120ms/piso (era ~600ms con recompute). Tracker incremental verificado contra recompute completo (0 mismatches/60 toggles). `_wall_span1()` = medidor de debug. **Bug aparte:** el gen base a veces deja una isla de piso aislada (~39 celdas) — preexistente. Detalle en `architecture_notes.md` (§ procgen dungeon_gen).

**Sesión 2026-06-29.quater (sin commitear):** (1) **fix bug histórico del rig del player**: el `z_index` de los hijos del `Rig` (CanvasGroup) se FUGABA al mundo → en `idle_north`/`walk_north` (z=5) y `walk_east/west` (z=3) el cuerpo+vara+mano se dibujaban sobre muros/props y cambiaba la calidez del cuerpo (perdía el tinte de luz del pie). Fix: todos los `Body:z_index` de `mage_anims.tres` → 0 (con `.bak`), y el apilado cuerpo↔vara ahora por **orden de árbol** (`player._set_body_front` con `rig.move_child`), todo el rig en z 0 → aplana atómico. Ver `architecture_notes.md §5`. (2) **Sandbox `scenes/props_test.tscn`** (+ `props_test.gd`): cuarto cerrado con props a mano para probar oclusión; usa `DungeonProps.spawn_at` (público). (3) Props **cuelgan de Main** (mismo y_sort que el player) → te tapan OPACOS si estás detrás; sin cutaway de props; default escala `384/w` y `props.json` viejo subido ×3. (4) **OCLUSIÓN X-AWARE por HUELLA** (Felipe: "AHHH FINALMENTE"): estás detrás del prop si, a tu columna X, la huella se extiende al sur de tus pies (sigue el borde inclinado iso; un escalar único sobre-tapaba por el costado). Por-frame: `DungeonProps.update_occlusion`/`_player_behind` setea el `z_index` del prop arriba/abajo del player; huella guardada en world al spawn; props sin huella → y_sort estático (`ysort` manual). Patrón reusable (memoria `reference_iso_ysort_footprint`). Detalle en `architecture_notes.md §1.props/§5`.

**Sesión 2026-06-29.ter (sin commitear):** (1) **minimapa → AUTOMAP estilo Diablo II**: wireframe iso de líneas (no relleno) — `minimap.gd` reescrito + nuevo `minimap_wire.gd` (`MinimapWire`), aristas de `Dungeon.get_wall_edges()`, radar circular + mapa M overlay translúcido. (2) Dos REGLAS nuevas de divisores en `dungeon_dividers.gd`: **ninguna región sin salida** (`ensure_connectivity`: flood-fill desde spawn, abre hueco en regiones selladas) + **sin puertas encimadas con muros** (`resolve_overlaps`: reubica la puerta a celda limpia; el render comparte el muro perimetral). Pipeline `_place_dividers` = plan → resolve_overlaps → ensure_connectivity → render. Compila headless OK (exit 0); falta que Felipe lo pruebe. Detalle en `docs/project_memory.md §7.ter` + `architecture_notes.md §3.bis/§3.ter`.

**Sesión 2026-06-29.bis (sin commitear):** **sistema de PROPS iso**. (1) Importado pack **Kenney Isometric** (144 PNGs, 4 facings E/N/S/W) a `assets/iso/props/kenney/`. (2) Tab web nueva **📦 Props** (`tools/props_tool.html`): seleccionás un asset y le fijás escala, anclaje (centro o borde de muro NW/NE/SE/SW + offset arrastrable), polígono de colisión (cell-local, igual que muros), weight y enabled → auto-guarda en `tools/rig/props.json` vía `GET/POST /api/props` (serve.py). Auto-descubre toda imagen de `assets/iso/props/**` (assets futuros aparecen solos). (3) Autoload **PropCatalog** + módulo **DungeonProps** (`place_props` en `generate()`) esparce props ponderados por el piso con anclaje + colisión + bloqueo de nav. Scripts parsean OK (find_symbols limpio); **falta** reimport de los PNGs (el editor estaba en play) y que Felipe lo pruebe. (4) **Posicionamiento z (final):** los props son **objetos sólidos** colgados de **`Main`** (mismo y_sort que el player) → el motor los ordena por profundidad contra el player: te parás detrás de una mesa/biblioteca y te **tapa opaca** (sin transparentar), adelante la tapás vos. Trade-off: player y muros no comparten y_sort → el prop y-sortea con el player (que te tape) y queda en bucket grueso vs muros (ok para muebles en pared). **NO** hay cutaway de props. Sprite + colisión ×`gs`(0.5) para calzar con tiles. Default de escala `384/w` (~1.5× tile); **`props.json` viejo tenía todo en 0.5 → subido ×3 a 1.5** (anclajes/colisiones preservados). Solo Kenney importado (Hand-Drawn descartado). Detalle en `docs/architecture_notes.md §1.props`.

**Sesión 2026-06-29 (sin commitear):** procgen — (1) **corredores isométricos**: `dungeon_gen._connect` talla la L por los ejes visuales del rombo (SE/SW) en vez de `carve_h`/`carve_v` cartesianos (escalera dentada); (2) **divisores que siempre topan muro**: `dungeon_dividers.add_divider` crece desde el centro hasta toparse vacío/borde en ambos extremos → no más divisores flotando en la nada. Compila headless OK (Godot 4.6.3, exit 0). Felipe verificó jugando ("perfecto ambos"). Detalle en `docs/project_memory.md §7.ter/§7.quater`.

**Sesión 2026-06-28 (autónoma, sin commitear):** fix crash ESC (audio.gd), fix luz de mobs inconsistente (cull por proximidad en `light_field.pack_lights`), luz del jugador con perímetro difuso (gaussiana en `player._apply_light_tex`), **minimapa centrado en el jugador** (radar con AtlasTexture en `minimap.gd`), **barra de habilidades RPG nueva** (`ability_defs.gd` + `ui/skill_bar.gd` + slots/cooldowns en `player.gd`, teclas 1-4 + K, AoE migró de E al slot 1), impacto de fuego rotado a la dirección (`projectile.gd`), XP bar 16px más abajo, botón debug de regen removido, clear color oscuro (mata el gris post-intro). Detalle + dudas abiertas en `docs/project_memory.md §7.bis`. Compila headless OK; falta que Felipe lo pruebe jugando.

Juego iso funcional y pulido, todo en `master`. `dungeon.gd` partido en **gen/decor/fog**, `hud` en paneles, **CombatDirector** autoload, mercader desacoplado por señal. Features recientes: **audio inmersivo** (buses+reverb+SFX posicional), **bolt de fuego por vara**, **FX de combate** (burn, números de daño, fogatas, luz propia de mobs), **boot splash**, **aura propia del boss** (se ve en la oscuridad), **FX de hechizos data-driven** (tab web ✨Hechizos `tools/spell_tool.html` → `tools/rig/spells.json` → `SpellLibrary`→`ElementProfile`→`SpellFX` por vara, modo `external` que aumenta el bolt existente sin pisar el arte; WYSIWYG, ver `docs/spell_fx_plan.md`). Deuda viva: nav marca piso-con-muro sólido, el boss nunca se oculta, sandboxes sin mover, tilesets `iso_dungeon_v*` chatarra. (`place_torches` cartesiano → **ARREGLADO** 2026-06-30.ter: ahora ancla a `_wall_segments` iso reales vía `spawn_wall_torch`.) Ver `docs/`.

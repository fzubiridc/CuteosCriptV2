# Backlog / cómo seguir — 2026-06-28

Propuesta priorizada tras la tanda de fixes de la sesión autónoma. Ordenado por **impacto/esfuerzo**.
No es un TODO obligatorio: es el mapa de "qué movería más la aguja" para que Felipe elija.

## A. Terminar lo que quedó de esta sesión (rápido, alto valor)
1. **Precargar `main.tscn` durante la intro** (mata el freeze post-intro de verdad). `narration_player.gd start()` → `ResourceLoader.load_threaded_request(next)`, `_finish()` → `change_scene_to_packed(load_threaded_get(next))`. + overlay `loading.png` en `main._ready` con `await get_tree().process_frame` antes de `_build_floor()`. (Lo dejé sin tocar porque el intro está en revisión narrativa.)
2. **Íconos de habilidades**: hoy son gemas placeholder generadas. Felipe dibuja `assets/ui/skills/{meteor,nova,blink,heal,frost,dash}.png` (48×48) y se enganchan solos.
3. **Limpiar dead code** de `player.gd`: `_try_aoe` / `_interactable_near` quedaron huérfanos al migrar el AoE al slot. Borrar + actualizar comentarios de `chest.gd`/`merchant.gd` ("el AoE del player (E)...").

## B. Quick wins gráficos desktop (del informe `webgl_to_desktop_upgrades.md`)
Son seguros pero VISUALES → que Felipe los apruebe con el ojo (es su dominio de arte).
4. **MSAA 2D** (hoy off): `project.godot rendering/anti_aliasing/quality/msaa_2d=1` (2×) → limpia los bordes diagonales del iso. Probar que no emborrone el pixel-art.
5. **`Engine.max_fps`** (ej. 144) para no quemar GPU en menús. No-visual.
6. **Glow**: bajar `glow_hdr_threshold` (1.2 está alto) para más bloom en bolts/auras/fuego. Visual, tunear.
7. **Estela del bolt CPU→GPU** (`projectile.gd:_trail` es el único `CPUParticles2D` que quedó; el resto ya es GPU).
8. **`shaders/post_fx.gdshader`**: código muerto (no cableado). Cablearlo (exposición/saturación, ya hay knobs) o borrarlo.

## C. Profundizar la barra de habilidades (la base ya está)
9. **Más habilidades con identidad**: cadena (rayo que salta entre mobs), escudo/absorción temporal, invocar fuego en línea, ralentizar de verdad (requiere un `slow_t`/`slow_mul` en `enemy.gd`). El registro `AbilityDefs` ya soporta agregarlas en una entrada + un `case` en `_cast_ability`.
10. **Slow real para "Estallido Glacial"**: hoy solo daña+empuja. Agregar `enemy.slow(mult, secs)` (reduce `move_speed` temporal) y un tinte celeste.
11. **Cooldown radial** (en vez del drenado vertical actual): shader de barrido circular sobre el ícono — más "AAA". Bajo esfuerzo, alto pulido.
12. **Recursos/loot que dan habilidades**: hoy las 6 están siempre disponibles. Idea: desbloquear por nivel / drop de tomos → progresión.

## D. Deuda técnica viva (de los docs previos, sigue)
13. **`DungeonDecor.place_torches` asume salas cartesianas** → antorchas mal puestas en salas iso (paralelogramos). Migrar a `spawn_wall_torch(cell, side)` por-borde.
14. **Nav AStarGrid**: "piso-con-muro" queda caminable (lo bloquea la colisión de perímetro, no el nav). Validar que los mobs no rocen bordes.
15. **Boss no entra al fog gating** (siempre visible). Decisión pendiente: ¿se oculta como los mobs?
16. **Visibilidad de mobs por DISTANCIA, no LOS** (disco `mob_reveal_dist`). A futuro: shadowcasting.
17. **Limpieza de chatarra**: tilesets `iso_dungeon_v*` (~21 `.tres`), escenas sandbox (`iso_test`/`closed_room_test`/`light_test`) + sus scripts huérfanos (`iso_procgen.gd`, `iso_mob_spawner.gd`, `iso_wall_occluder.gd`), `bossbar.gd`/`window_sky.gd`/`_autowalk.gd`.

## E. Gameplay / contenido (más grande, define el juego)
18. **Narrativa**: revisión a fondo (Felipe dijo que no convence). Usar `/cuteo-storyteller` + biblia v4. Mapear lore↔contenido real del juego.
19. **Balance por profundidad**: validar la curva de HP/daño/densidad; el daño de contacto del boss va sin `hit_cd`.
20. **Variedad de salas**: formas (L, cavernas, salas grandes), props por rol, eventos.
21. **Feedback de combate**: hitstop sutil, screen shake por crit, SFX por elemento de vara.
22. **Audio**: ya es inmersivo; faltaría música por zona/jefe y un stinger de victoria.

## F. Export / release (objetivo: .exe Windows)
23. **Pipeline de export desktop**: preset Windows, ícono, versión, firma opcional. (El workflow viejo de Pages/WebGL quedó histórico.)
24. **Menú de opciones**: volumen (buses World/Music ya existen), resolución/fullscreen, remapeo de teclas (el InputMap ya está armado).
25. **Guardado robusto**: save atómico (`.tmp`+rename), distinguir corrupto vs ausente.
